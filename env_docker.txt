# Docker Environment Configuration for Biometric Attendance System

APP_NAME="Biometric Attendance System"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration (Docker)
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=attendance_db
DB_USERNAME=attendance_user
DB_PASSWORD=60813322XXyyuv@
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# Redis Configuration (Docker)
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_DB=0

# Session and Cache
SESSION_DRIVER=redis
SESSION_LIFETIME=120
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis

# File Storage
FILESYSTEM_DISK=local

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Face Recognition API
FACE_API_KEY=
FACE_API_URL=

# Application Settings
ATTENDANCE_SESSION_TIMEOUT=30
FACE_VERIFICATION_THRESHOLD=0.8
MAX_IMAGE_SIZE=5120
ALLOWED_IMAGE_TYPES=jpg,jpeg,png

# Security Settings
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax

# Performance Settings
CACHE_TTL=3600
VIEW_CACHE_TTL=3600
ROUTE_CACHE_TTL=3600

# Logging Settings
LOG_MAX_FILES=30
LOG_LEVEL_DAILY=debug

# Backup Settings
BACKUP_RETENTION_DAYS=30
BACKUP_COMPRESSION=true

# Monitoring Settings
ENABLE_MONITORING=true
HEALTH_CHECK_INTERVAL=300 